 //exercicees 1 :
// List<string> keys = new List<string> { "Ten", "Twenty", "Thirty" };
// List<int> values = new List<int> { 10, 20, 30 };
// var diction = keys.Zip(values, (k, v)  => new {Key=k, Value=v}).ToDictionary(item => item.Key, item => item.Value);

// Console.WriteLine("{");
// foreach (var t in diction)
// {
//     Console.Write($"\"{t.Key}\" , {t.Value} ");
// }
// Console.WriteLine("}");

// exercice 2 :

